# Production stack for GuessEat.
# Used by Coolify or standalone docker. Mobile builds are NOT here (use EAS).
#
# Usage:
#   docker compose up -d
#
# All env vars are injected by Coolify (via Environment Variables UI).
# Coolify creates a .env file and passes it via --env-file.
# The env_file: .env directive below loads those vars into each container.

services:
  postgres:
    image: postgis/postgis:16-3.5
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ${DB_DATABASE:-guesseat}
      POSTGRES_USER: ${DB_USERNAME:-guesseat}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-guesseat}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--maxmemory", "200mb", "--maxmemory-policy", "allkeys-lru"]
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./packages/api
      args:
        ROLE: api
    restart: unless-stopped
    env_file: .env
    environment:
      DB_HOST: postgres
      REDIS_HOST: redis
      ROLE: api
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    expose:
      - "8080"

  queue:
    build:
      context: ./packages/api
      args:
        ROLE: queue
    restart: unless-stopped
    env_file: .env
    environment:
      DB_HOST: postgres
      REDIS_HOST: redis
      ROLE: queue
    depends_on:
      - api

  scheduler:
    build:
      context: ./packages/api
      args:
        ROLE: scheduler
    restart: unless-stopped
    env_file: .env
    environment:
      DB_HOST: postgres
      REDIS_HOST: redis
      ROLE: scheduler
    depends_on:
      - api

  # Reverb WebSocket server (Phase 3 — Duels). Uncomment when enabling.
  # reverb:
  #   build:
  #     context: ./packages/api
  #     args:
  #       ROLE: reverb
  #   restart: unless-stopped
  #   env_file: .env
  #   environment:
  #     DB_HOST: postgres
  #     REDIS_HOST: redis
  #     ROLE: reverb
  #   depends_on:
  #     - api
  #   expose:
  #     - "8080"

  web:
    build:
      context: .
      dockerfile: ./packages/web/Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-}
    restart: unless-stopped
    depends_on:
      - api
    expose:
      - "80"

volumes:
  postgres_data:
  redis_data: